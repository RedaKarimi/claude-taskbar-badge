# Build artifacts that ship in releases but should not be committed to source
# (regenerated by ps2exe)
# Comment these two lines if you want the prebuilt exes in the repo.
# install.exe
# uninstall.exe

# Runtime state
*.log
*.tmp

# Editor / OS junk
.vscode/
.idea/
Thumbs.db
Desktop.ini

# Private notes
NOTES.local.md
